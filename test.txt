const shopWa = toPeruE164(currentProduct?.shop?.whatsapp_number) || "51922540931";
const text = `Hola, ya realicé el pago por *${currentMethod}* del producto *${currentProduct.name}* por el monto de S/ ${currentProduct.price.toFixed(2)}. Adjunto comprobante.`;
notifyBtn.href = `https://wa.me/${shopWa}?text=${encodeURIComponent(text)}`;