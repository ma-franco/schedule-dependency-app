let allProducts = [];
const exchangeRate = 3.8;

// Función para mostrar errores en el grid
function showError(msg) {
  const grid = document.getElementById("products-grid");
  if (!grid) return;
  grid.innerHTML = `<div class="text-white bg-red-600/80 p-3 rounded">${msg}</div>`;
}